# 데이터 입력하기(1)

### 데이터 저장: `INSERT`

테이블에 새로운 데이터를 추가할 때 사용

```sql
INSERT INTO 테이블명 () VALUES ();
```

<br>

### 데이터 무결성

데이터의 **정확성과 일관성을 유지**하기 위한 규칙

| 구분 | 의미 | 관련 제약조건 |
|---|---|---|
| 엔티티 무결성 | 각각의 행을 고유하게 식별 | `PRIMARY KEY` |
| 도메인 무결성 | 필드에 허용되는 값의 범위를 제한 | 데이터 타입, `NOT NULL` |
| 참조 무결성 | 테이블 간의 올바른 참조 관계 유지 | `FOREIGN KEY` |

<br>

### `PRIMARY KEY`: 테이블의 각 행을 **고유하게 식별하기 위한 기본키**

- 중복된 값을 가질 수 없음
- `NULL` 값을 가질 수 없음
- 하나 이상의 필드로 구성
- 하나의 테이블에는 하나의 `PRIMARY KEY`만 존재

> 이미 존재하는 기본키 값을 다시 입력하면 오류 발생

<br>

### `AUTO_INCREMENT`: 새로운 데이터가 입력될 때 **숫자 값을 자동으로 증가시켜 저장하는 속성**

`AUTO_INCREMENT`가 설정된 필드에 `NULL`을 입력하면 다음 번호가 자동으로 생성됨

<br>

### `NULL`: **값이 존재하지 않는 상태**

`NULL`을 허용하는 필드는 직접 `NULL`을 입력하거나 생략 가능

<br>

### `NOT NULL`: **반드시 값이 입력되도록 제한하는 제약조건**

`NOT NULL`이 설정된 필드는 `NULL`을 입력하거나 생략할 수 없음

<details>
<summary><h2>연습문제</h2></summary>

### 01

학과 테이블(departments)에 소프트웨어공학과를 새로 등록하세요. 학과 번호는 자동으로 생성되도록 하며,  
학과명은 '소프트웨어공학과', 위치는 '2호관', 전화번호는 '051-123-7890', 이메일은 'sw@myschool.ac.kr',  
설립연도는 2012, 홈페이지는 'http://sw.myschool.ac.kr'로 입력하세요.

```sql

```

### 02

방금 등록한 소프트웨어공학과에 소속된 학생 1명을 students 테이블에 추가하세요.  
학생의 이름은 '박세리', 학년은 2학년이며, 생년월일은 2005년 10월 17일입니다.  
키는 169cm, 몸무게는 56kg, 성별은 '여', 학적 상태는 '재학'입니다.  
사용자 아이디(user_id), 주민번호(idnum), 연락처는 임의로 지정하세요.

```sql

```

### 03

소프트웨어공학과에 소속된 교수 1명을 professors 테이블에 등록하세요.  
이 교수의 이름은 '최정훈'이며, 아이디는 'jchhoi', 직급은 '부교수'입니다.  
급여는 5,200,000원이며, 입사일은 2018년 3월 2일, 재직 상태는 '재직'입니다.  
소속 학과는 앞에서 생성한 학과와 동일하게 설정하세요. 이메일, 연락처, 사진의 URL은 Null로 저장하세요.

```sql

```

### 04

소프트웨어공학과에서 개설하는 과목으로 '소프트웨어프로젝트실습'을 추가하세요.  
학점은 3이며, 개설 학과 번호는 소프트웨어공학과로 설정하세요. 담당 교수는 아직 정해지지 않았습니다.

```sql

```

### 05

학생 번호 10101번이 '웹프로그래밍' 과목을 2025년 3월 2일에 수강신청하였습니다.  
이 학생의 점수는 아직 부여되지 않았습니다. 이 정보를 enrollments 테이블에 등록하세요.

```sql

```

</details>
